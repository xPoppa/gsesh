# Gsesh ghostty session manager

to install run:
```bash
go run install.go
```

copy the output to your systemd folder and run as
```bash
systemctl --user start gsesh.service
```

Than add ~/.local/bin to your PATH
ready to go!
